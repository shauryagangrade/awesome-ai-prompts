# CODEOWNERS - required reviewers for all files in the repo.
# Effective only with "Require review from Code Owners" enabled in branch
# protection for main (Settings > Branches > Rule).
# https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners

# A single owner owns everything today, so the catch-all rule below is what is
# enforced. Directory-level rules for the areas most likely to gain a dedicated
# owner (CI/governance, tooling/scripts) are listed explicitly so assigning one
# is a one-line change per directory. Prompt categories and docs stay on the
# catch-all until a per-category owner exists.
* @shauryagangrade
/.github/ @shauryagangrade
/scripts/ @shauryagangrade
